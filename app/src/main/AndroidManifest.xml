<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.attila.gerendi.pocketsigns">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketSigns">
        <activity
            android:name=".EulaActivity"
            android:exported="false"
            android:label="@string/title_activity_eula"
            android:theme="@style/Theme.PocketSigns.NoActionBar" />
        <activity
            android:name=".FullScreenViewActivity"
            android:exported="false"
            android:theme="@style/Theme.PocketSigns.Fullscreen" />
        <activity
            android:name=".CategoryActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>